# Current State and Modernization Tracks

## Goal of this chapter

This chapter separates what is already landed in the current mainline runtime from what remains partially landed, optional, or research-oriented.

## Mainline state

The strongest current architectural result in the repository is the typed-slot runtime path over the fixed typed `W=8` machine. This includes:

- explicit `SlotClass` taxonomy;
- class-capacity accounting;
- two-stage typed-slot admission (`TryClassAdmission` then `TryMaterializeLane`);
- deterministic late lane binding;
- replay-aware stable-donor lane reuse;
- legality authority routed through explicit `LegalityDecision` values;
- telemetry for inject/reject outcomes;
- compiler/runtime typed-slot agreement checks.

This is not just an idea documented in README. The corresponding classes and runtime paths are present in the live scheduler and safety layers.

## Canonical topology reminder

All modernization tracks in this chapter sit on the same canonical lane map:

- `ALU[0..3]`
- `LSU[4..5]`
- `DMA/stream[6]`
- `Branch/System[7]`

The lane-7 branch/system alias is part of the active scheduling model, not an incidental implementation detail.

## Current-state matrix

| Area | Implemented behavior | Model/helper-only or compatibility projection | Denied / fail-closed behavior | Future work |
|---|---|---|---|---|
| Base CPU/VLIW/typed-slot | Fixed `W=8`, 4-way SMT, typed slot classes, deterministic late lane binding, replay-aware scheduling, runtime legality, scalar/load-store/atomic/fence contours named in the instruction-refactor pack | Typed-slot facts remain validation/compatibility transport unless stricter policy is selected | Unknown/prohibited opcodes, retired compat policy bits, unsupported masks/payloads, stale compiler versions | Stronger compiler-side typed-slot adoption and broader public spec split |
| Virtualization | Neutral runtime descriptors/services, frozen VMX compatibility frontend, generated VMREAD projection for admitted completion/memory/execution fields, VMCALL admitted-denied trap/backend/route fences | VMX, VMCS/VMCSv2, `VmExitReason`, `VmxCaps`, and VMREAD values are compatibility/projection vocabulary | VMX backend authority, mutable VMCS store, VMCS writes, ownerless privileged/control/host VMREAD fields, successful VMCALL backend publication, VMX-owned SecureCompute | Neutral backend owners, host/privileged owners, control projection contract, nested runtime paths |
| Stream, MatrixTile, lane6/lane7 | VectorStream helpers; closed MatrixTile memory/compute runtime; lane6 DSC1 contour; L7-SDC lane7 command contour | DSC2 parser/model, generic helper APIs, telemetry, backend diagnostics, queue/fence/register ABI models | MTILE ordinary-LSU/DSC fallback; DSC2 execution; async/coherent DMA; VMX/custom-backend fallback | Future MTILE extensions, DSC2/queue/coherency/IOMMU work, broader L7 protocol |
| SecureCompute | `SecureComputeDomainDescriptor`, disabled/no-effect semantics, `SecurityLevel.None -> Disabled`, optional runtime context binding, secure memory/measurement/evidence/migration/I/O/hypercall policies, Layer 2 grant discipline, VMX boundary matrix | VMX read-only projection may be allowed only as projection vocabulary; Post-Phase10 backend owner gate can accept proof only | Secure VMCS, VMX activation/grant, `VmxCaps` authority, VMCS checkpoint ownership, CHERI/tagged memory reinterpretation, positive secure backend runtime execution | Plan2 open decisions: backend owner, compatibility advertisement, alias placement, capability-aware ISA, tag/provenance migration |

## Active frontend boundary

The active frontend is native VLIW only. Compatibility ingress remains quarantined to compat or diagnostic seams, and DBT or scalar-generalized decode are not active proof surfaces in the current repository.

Boundary evidence is already present in the live suite surface:

- `Phase11NativeVliwDiagnosticDecodeTests.cs`
- `Phase12VliwCompatFreezeTests.cs`
- `Phase12RetiredCompatPolicyBitBoundaryTests.cs`

## Track A: Typed-slot runtime modernization

The following runtime capabilities are concretely present in code:

- `SlotClassDefinitions.cs`: topology, pinning, reject taxonomy, bundle facts
- `SlotClassCapacity.cs`: occupancy and remaining-capacity model
- `MicroOpScheduler.Admission.cs`: class admission plus deterministic lane materialization
- `DeterministicLaneChooser.cs`: lowest-free plus replay-hint selection
- `MicroOpScheduler.SMT.cs`: typed-slot path integrated into live intra-core SMT packing
- `SafetyVerifier.TypedSlot.cs`: fact validation and agreement checks

The architectural shift is from "fill an exact empty physical slot if possible" to "admit into class capacity, then bind a legal lane deterministically."

## Track B: Execution evidence and replay-oriented modernization

The second strong line of implementation is the evidence-oriented execution complex:

- loop-buffer replay phase context;
- replay-aware legality reuse;
- phase-certified bundle packing;
- boundary guards and serializing invalidation;
- determinism and lane-reuse telemetry;
- trace and telemetry export;
- replay token capture/rollback substrate.

This track strengthens the scheduler and pipeline without turning the system into a nondeterministic hidden scheduler.

## Track C: Instruction-runtime closure

The completed instruction-refactor phases add a concrete ISA-runtime closure track on top of the typed-slot and replay modernization:

- scalar integer64 repair is closed through `ZEXT.W = 321`;
- canonical branch/control target transport uses `Immediate`, with target sideband rejected;
- typed scalar load/store semantics cover sign/zero extension, store truncation, exact footprints, and retire-owned commit;
- LR/SC and AMO W/D retire semantics are closed;
- atomic acquire/release ordering is proven for the currently published carriers;
- canonical zero-payload `FENCE` and `FENCE_I` are bounded lane7/system retire events;
- vector indexed/2D, gather/scatter, cache/TLB/coherency, and broad lane6/lane7
  contours beyond current gated slices remain explicit fail-closed or
  future-gated surfaces. The four-instruction MatrixTile contour is closed.

This track is important because it prevents the architecture narrative from treating opcode rows, compiler helpers, parser awareness, or metadata as execution evidence.

## Backend truthfulness boundary

The live backend retains explicit rename/commit state. Current runtime state explicitly includes:

- `PhysicalRegisterFile`
- `RenameMap`
- `CommitMap`
- `FreeList`

That backend substrate coexists with legality, certificate, and typed-slot machinery. The current architectural claim is therefore not "rename elimination," but rather that bundle legality and certificate-governed admission are made explicit and analyzable instead of being buried inside a hidden scheduler policy surface.

## What is landed but still optional or guarded

Some mechanisms exist in code but are clearly optional, conditional, or exploratory:

- `EnableShadowOracle` instrumentation in the scheduler
- pipelined FSP mode
- optional scalar residual and future matrix-extension contours, which remain
  rejected unless separately enabled
- descriptor-only `VGATHER` / `VSCATTER`, Lane6, and Lane7 carriers
- reserved cache/TLB/coherency rows
- execution-surface guards for stream control and VMX routing when surfaces are not wired

These must not be described as unconditional production behavior.

## What is not yet fully landed end-to-end

A key boundary remains between runtime readiness and compiler adoption:

- runtime code contains typed-slot facts and agreement checks;
- runtime comments explicitly say correct execution does not require non-default `SlotMetadata`;
- the compiler-side story is not yet "compiler facts are mandatory for correctness."
- runtime proof for atomic/fence behavior does not automatically open compiler atomic or broad fence lowering.

That means the runtime modernization is ahead of the full compiler-mainline adoption story.

## Repository entry points

The root `README.md` is now a short canonical entry point rather than a paper-style architecture dump.
It links readers to the current authority surfaces for operational semantics, legality, typed-slot contract staging, replay, memory/exception semantics, backend truthfulness, and validation evidence.

The WhiteBook still remains the deeper architecture-facing narrative, but README no longer needs to be treated as an outdated or competing authority surface.

## What is authoritative in code

Primary authority sources:

- `HybridCPU_ISE/Core/Pipeline/Scheduling/`
- `HybridCPU_ISE/Core/Pipeline/Safety/`
- `HybridCPU_ISE/Core/Pipeline/Components/LoopBuffer.cs`
- `HybridCPU_ISE/Core/Diagnostics/`
- `HybridCPU_ISE/Core/Contracts/CompilerContract.cs`
- `Documentation/InstructionsRefactor/WhiteBook/`

Representative test surfaces:

- `HybridCPU_ISE.Tests/tests/Phase09RuntimeLegalityServiceReachabilityProofTests.cs`
- `HybridCPU_ISE.Tests/tests/Phase09ReplayCertificateCoordinatorProofTests.cs`
- `HybridCPU_ISE.Tests/tests/Phase12CompilerContractHandshakeTests.cs`
- `HybridCPU_ISE.Tests/tests/Phase11DiagnosticsTracingTests.cs`

## Current limitations

- Full compiler-side typed-slot fact emission is not yet required for runtime correctness.
- Some modernization lines are present as guarded or exploratory instrumentation rather than as the sole runtime authority.
- Historical notes remain in the tree, but they are explicitly non-authoritative and should not outrank the current README or WhiteBook entry points.

## Open questions / future work

- When and how typed-slot facts become default compiler output rather than optional or diagnostic transport.
- Whether shadow-oracle instrumentation graduates into a stronger validation or tuning workflow.
- Whether the repository should collapse more repository-facing summaries onto the same small set of authoritative architecture and validation entry points.
