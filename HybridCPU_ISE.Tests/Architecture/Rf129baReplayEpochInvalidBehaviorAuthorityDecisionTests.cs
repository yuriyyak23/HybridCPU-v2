namespace HybridCPU_ISE.Tests.Architecture;
public sealed class Rf129baReplayEpochInvalidBehaviorAuthorityDecisionTests
{[Fact] public void PaperConstrictsInvalidCutoverToActiveZeroSchedulerIngress(){string p=File.ReadAllText(Path.Combine(Root(),"ResearchPaper","section","md base","7_Replay_Stable_Placement_Replay_Tokens_and_Execution_Boundaries.md"));Assert.Contains("active raw phase context with epoch zero is malformed",p,StringComparison.Ordinal);Assert.Contains("before replay-aware telemetry",p,StringComparison.Ordinal);Assert.Contains("existing inactive-phase invalidation contour",p,StringComparison.Ordinal);Assert.Contains("active-zero ingress boundary",p,StringComparison.Ordinal);Assert.Contains("cannot itself prove loop-buffer",p,StringComparison.Ordinal);Assert.Contains("not a license to add an inferred",p,StringComparison.Ordinal);}private static string Root(){DirectoryInfo? c=new(AppContext.BaseDirectory);while(c is not null){if(Directory.Exists(Path.Combine(c.FullName,"ResearchPaper")))return c.FullName;c=c.Parent;}throw new DirectoryNotFoundException();}}
