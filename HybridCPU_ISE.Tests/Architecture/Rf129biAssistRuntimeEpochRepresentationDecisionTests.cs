namespace HybridCPU_ISE.Tests.Architecture;
public sealed class Rf129biAssistRuntimeEpochRepresentationDecisionTests
{[Fact] public void PaperRetainsAllRepresentableLocalFreshnessAsOwnerPrivateRawState(){string p=File.ReadAllText(Path.Combine(Root(),"ResearchPaper","section","md base","6_Assist_Coupled_Data_Movement_and_Donor_Semantics.md"));Assert.Contains("every raw `UInt64` bit pattern is a",p,StringComparison.Ordinal);Assert.Contains("create no representational",p,StringComparison.Ordinal);Assert.Contains("retains its owner-private raw storage",p,StringComparison.Ordinal);Assert.Contains("`AssistRuntimeEpoch` checked type",p,StringComparison.Ordinal);Assert.Contains("not an ownership transfer",p,StringComparison.Ordinal);}private static string Root(){DirectoryInfo? c=new(AppContext.BaseDirectory);while(c is not null){if(Directory.Exists(Path.Combine(c.FullName,"ResearchPaper")))return c.FullName;c=c.Parent;}throw new DirectoryNotFoundException();}}
